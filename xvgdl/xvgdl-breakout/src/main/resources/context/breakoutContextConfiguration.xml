<?xml version="1.0" encoding="UTF-8" standalone="no" ?>
<!-- Definition of pacman game -->

<gameDefinition>

    <!-- Renderer configuration -->
    <property key="rendererConfiguration"
        value="/context/breakoutAsciiRendererConfiguration.xml" />
    <!-- Timeout configuration in milliseconds. Set to -1 for no timeout -->
    <property key="TIMEOUT" value="-1" />

    <layout>
        <component id="gameInfoTop" location="TOP">
            <contextProperty id="score" />
            <contextProperty id="hiScore" />
        </component>

        <component id="gameInfoDown" location="BOTTOM">
            <contextProperty id="lives" />
        </component>
    </layout>

    <!-- Map Definition -->
    <map type="2D" sizeX="55" sizeY="90" toroidal="false"
    	file="/context/breakoutmap.txt">
    </map>

    <!-- Players Definition -->
    <players number="1" maxNumber="1" minNumber="1">
        <player name="platform" score="0" lives="3" sizeX="10" sizeY="1"/>
    </players>
    <!-- Defining the Objects present in the game (NPCs, Obsctacles, projectiles,
        structures, collectibles, etc... -->
    <objects>
        <object name="wall" type="wall" dynamic="false" volatile="false" sizeX="1" sizeY="1" />
        <object name="brick" type="enemy" dynamic="false" volatile="true" sizeX="1" sizeY="1"  />
        <object name="goal" type="item" dynamic="false" volatile="true" sizeX="1" sizeY="1"  />
        <object name="missile" type="projectile" dynamic="true" volatile="false" sizeX="1" sizeY="1"  />
    </objects>

    <!-- Defining Game physics. Movements and collisions Define set of possible
        physics action: gravitational pull, friction, repulsion forces, bouncing
        effects, stickiness, etc Define the -->
    <physics>
    </physics>

    <!-- Defining game events. Events sent from the player via input devices,
        timed or random events, and object collision triggered events that affect
        the game. May be as an example some item appears randomly. It can use a timer
        (not before X milliseconds) or a random value to be executed -->
    <events>
        <event type="engine" className="es.jor.phd.xvgdl.model.event.SpawnItemEvent"
            objectName="brick" timer="15000" />
    </events>

    <!-- Defining rules. Events consequences -->
    <rules>

        <!-- Rules for missile hitting bricks -->
        <rule name="missileHitsBrick" type="collision">
            <ruleAction objectName="missile" result="scoreUp" value="100" />
            <ruleAction objectName="brick" result="disappear" />
        </rule>

        <!-- Rules for missile hitting walls -->
        <rule name="missileHitsWall" type="collision">
            <ruleAction objectName="missile" result="changeDirection" value="random" />
            <ruleAction objectName="wall" result="" />
        </rule>

        <!-- Rules for missile hitting walls -->
        <rule name="missileHitsPlatform" type="collision">
            <ruleAction objectName="missile" result="changeDirection" value="invert" />
            <ruleAction objectName="platform" result="" />
        </rule>

        <!-- Rules for missile hitting walls -->
        <rule name="missileHitsGoal" type="collision">
            <ruleAction objectName="missile" result="teletransport" value="platform" />
            <ruleAction objectName="platform" result="" />
        </rule>

    </rules>
    <!-- Game End Conditions -->
    <endConditions>
        <endCondition checkerClass="es.jor.phd.xvgdl.model.endcondition.TimeoutGameEndCondition" property="timeout" value="0"/>
        <endCondition checkerClass="es.jor.phd.xvgdl.model.endcondition.NoObjectsPresentGameEndCondition" objectNames="brick" />
        <endCondition checkerClass="es.jor.phd.xvgdl.model.endcondition.LivesZeroGameEndCondition" />
    </endConditions>
</gameDefinition>
